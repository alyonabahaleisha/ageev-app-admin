rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // Public read for all media files (mobile apps stream audio/images)
    match /{allPaths=**} {
      allow read: if true;
      allow write: if request.auth != null;
    }
  }
}
